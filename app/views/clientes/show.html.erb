<div class="container">
  <div class="row">
    <div class="col-md-6 offset-md-3">
      <p style="color: green"><%= notice %></p>
      <div class="card">
        <div class="card-body">
          <h1 class="card-title"><%= @cliente.nombre %></h1>
          <p class="card-text">Email: <%= @cliente.email %></p>
        </div>
      </div>

      <div class="mt-3 d-flex justify-content-center">
        <%= link_to "Editar Cliente", edit_cliente_path(@cliente), class: "btn btn-primary mr-2" %>
        <%= link_to "Volver a la Lista de Clientes", clientes_path, class: "btn btn-secondary mx-2" %>
        <%= button_to "Eliminar Cliente", @cliente, method: :delete, data: { confirm: "¿Estás seguro de que deseas eliminar este cliente?" }, class: "btn btn-danger ml-2" %>
      </div>
    </div>
  </div>
</div>
